[pytest]
testpaths = 
    app/modules
    app/core/auth
pythonpath = .
asyncio_mode = auto
addopts = 
    -v
    --strict-markers
    --tb=short
    --disable-warnings
    --import-mode=importlib
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests