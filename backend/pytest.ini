[pytest]
testpaths = app/modules
pythonpath = .
addopts = 
    -v
    --strict-markers
    --tb=short
    --disable-warnings
    --import-mode=importlib
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests